# 前端架構設計

|項目|內容|
|----|-----|
|文件版本|v1.5|
|撰寫日期|2026-05-29|
|依據文件|`02-使用者需求.md`、`04-API設計.md`|
|框架|Next.js 15（App Router）|
|UI 元件庫|shadcn/ui|
|伺服器狀態|TanStack Query（React Query v5）|
|認證|NextAuth（已由 Vexa 建立）|

---

## 一、技術選型

| 層次 | 選擇 | 理由 |
|------|------|------|
| 框架 | Next.js 15 App Router | SSR + 快速路由切換，`layout.tsx` 實現巢狀 UI |
| UI 元件 | shadcn/ui + Tailwind CSS | 符合 `01-專案目標.md` 規範；無頭元件易於客製 |
| 伺服器狀態 | TanStack Query v5 | API 快取、背景輪詢（索引狀態、逐字稿）、樂觀更新 |
| 表單驗證 | React Hook Form + Zod | 型別安全，與後端 schema 共用驗證規則 |
| 圖示 | Lucide React | shadcn/ui 預設使用，風格一致（**⚠️ 設計備注**：避免過度 AI 感的圖示；基於 Vexa 授權條款，不得在 UI 中展示 Vexa 商標或 Logo） |
| 檔案上傳 | react-dropzone | 拖拉、點擊、剪貼簿上傳 |
| 通知 | sonner | shadcn/ui 整合的 Toast |

### 認證 Token 策略（與 04-API設計.md 補充）

```
useSession() → session.vexaToken（Vexa API token）
  ↓
呼叫 App Backend 時：Authorization: Bearer <vexaToken>
  ↓
Backend 驗證：查詢 public.api_tokens WHERE token = vexaToken → 取得 user_id
```

這比 NextAuth JWT 更簡單：`vexaToken` 已在 `session` 中，無需額外處理。

---

## 二、資料夾結構

```
src/
├── app/
│   ├── (auth)/                        # 不需要 App Shell 的頁面群組
│   │   └── login/
│   │       └── page.tsx               # 登入頁（沿用 Vexa 原本實作）
│   │
│   ├── (app)/                         # 需要 App Shell（Sidebar）的頁面群組
│   │   ├── layout.tsx                 # App Shell：認證守衛 + Sidebar
│   │   ├── page.tsx                   # / → redirect to /projects
│   │   ├── meetings/
│   │   │   ├── page.tsx               # /meetings：全局會議列表（跨專案 + 無關聯專案）
│   │   │   └── [meetingId]/
│   │   │       └── page.tsx           # /meetings/:id：無關聯專案的會議詳情
│   │   └── projects/
│   │       ├── page.tsx               # /projects：專案總覽
│   │       └── [projectId]/
│   │           ├── layout.tsx         # 專案 Shell：麵包屑 + 分頁導覽
│   │           ├── page.tsx           # /projects/:id → redirect to materials
│   │           ├── materials/
│   │           │   └── page.tsx       # 資料管理頁
│   │           ├── meetings/
│   │           │   ├── page.tsx       # 會議清單頁
│   │           │   └── [meetingId]/
│   │           │       └── page.tsx   # 會議詳情頁
│   │           ├── members/
│   │           │   └── page.tsx       # 成員管理頁
│   │           └── history/
│   │               └── page.tsx       # 編輯歷史頁
│   │
│   └── api/
│       └── auth/
│           └── [...nextauth]/
│               └── route.ts           # NextAuth handler（沿用 Vexa）
│
├── components/
│   ├── ui/                            # shadcn/ui 原始元件（不手動修改）
│   ├── layout/
│   │   ├── app-shell.tsx              # 整體佈局（Sidebar + Content）
│   │   ├── sidebar.tsx                # 左側導覽（含 Projects、Meetings、全局建立會議）
│   │   └── project-tabs.tsx          # 專案內的分頁導覽列
│   ├── projects/
│   │   ├── project-card.tsx           # 專案清單項目卡片
│   │   ├── create-project-dialog.tsx  # 建立專案的對話框
│   │   └── project-filter-bar.tsx     # 搜尋欄 + 類型篩選 + 排序
│   ├── materials/
│   │   ├── material-table.tsx         # 資料清單表格
│   │   ├── material-row.tsx           # 單筆資料列（含狀態 Badge）
│   │   ├── upload-zone.tsx            # 拖拉上傳區域
│   │   ├── upload-staging-dialog.tsx  # 暫存確認對話框
│   │   └── indexing-status-badge.tsx  # 索引狀態 Badge（含自動輪詢）
│   ├── meetings/
│   │   ├── meeting-list.tsx           # 會議清單（專案內 + 全局共用）
│   │   ├── meeting-row.tsx            # 單筆會議列
│   │   ├── meeting-filter-bar.tsx     # 搜尋欄 + 時間篩選 + 排序
│   │   ├── create-meeting-dialog.tsx  # 建立會議的對話框（含關聯專案選擇）
│   │   ├── live-transcript.tsx        # 即時逐字稿（含輪詢）
│   │   ├── meeting-summary.tsx        # 摘要 + 交辦事項
│   │   └── bot-status-indicator.tsx   # Bot 狀態圖示
│   └── members/
│       ├── member-list.tsx            # 成員清單
│       ├── member-row.tsx             # 單筆成員列（含權限 Badge）
│       └── invite-member-dialog.tsx   # 邀請成員的對話框
│
├── hooks/                             # 封裝 TanStack Query 的自訂 Hook
│   ├── use-projects.ts
│   ├── use-project.ts
│   ├── use-materials.ts
│   ├── use-meetings.ts                # 專案內會議清單
│   ├── use-all-meetings.ts            # 全局 Meetings 頁面（跨專案 + 無專案）
│   ├── use-meeting.ts
│   ├── use-transcriptions.ts          # 含輪詢邏輯
│   ├── use-members.ts
│   └── use-permissions.ts             # 從 project 推導當前使用者的權限
│
├── lib/
│   ├── api-client.ts                  # fetch 封裝（自動帶 Authorization header）
│   ├── auth.ts                        # NextAuth config（authOptions）
│   └── utils.ts                       # cn()、formatDate() 等工具函式
│
└── types/
    └── api.ts                         # 所有 API 回應的 TypeScript 型別定義
```

---

## 三、路由與頁面責任

### 3.1 `/projects` — 專案總覽

**責任**：顯示使用者參與的所有專案、提供建立新專案入口。Sidebar 提供全局「建立會議」與 Meetings 入口。

```
┌─ Sidebar ─────────┬─ Main Content ────────────────────────────────┐
│                   │                                               │
│  🤖 MeetBot       │  我的專案              [+ 建立專案]            │
│                   │  🔍 搜尋...  [全部▼]  [倒序▼]                 │
│  [+ 建立會議]     │                                               │
│                   │  ┌───────────────────────────────────────────┐│
│  📁 Projects      │  │ 🏢 Q3 產品規劃  Owner  3成員  7份資料      ││
│  🗂 Meetings      │  │    1個進行中會議                  [進入 →] ││
│                   │  ├───────────────────────────────────────────┤│
│  ── 最近專案 ──   │  │ 📁 行銷企劃    成員  5成員  12份資料       ││
│  ▶ Q3 產品規劃   │  │    無進行中會議                  [進入 →] ││
│    行銷企劃       │  └───────────────────────────────────────────┘│
│                   │                                               │
│  ─────────────    │                                               │
│  👤 user@..       │                                               │
└───────────────────┴───────────────────────────────────────────────┘
```

> **Sidebar 說明**：
> - **`[+ 建立會議]`**：全局入口，開啟 `GlobalCreateMeetingDialog`（含關聯專案選擇）
> - **`📁 Projects`**：切換至 `/projects` 頁面
> - **`🗂 Meetings`**：切換至 `/meetings` 頁面（全局會議列表）
> - 搜尋欄 + `[全部▼]`（類型篩選）+ `[倒序▼]`（排序）在主內容區

**元件樹**
```
ProjectsPage
├── ProjectFilterBar         ← search、type、order 篩選
└── ProjectCard[]            ← GET /projects（帶篩選參數）
    └── CreateProjectDialog (modal)

Sidebar
└── GlobalCreateMeetingDialog  ← POST /meetings（projectId 選填）
    └── ProjectSelector         ← GET /projects（篩選具 canMeeting 的專案）
```

---

### 3.2 `/projects/[projectId]` — 專案 Shell Layout

**責任**：渲染專案名稱、分頁導覽列，並根據使用者權限顯示/隱藏特定分頁。

```
┌─ Sidebar ─────┬─ Project Shell ────────────────────────────┐
│               │  Q3 產品規劃                  [⚙ 設定▼]   │
│  ▶ Q3 產品    │  [📄 資料]  [📹 會議]  [👥 成員]  [📋 歷史] │
│    行銷企劃   ├─────────────────────────────────────────────┤
│               │  〈子頁面內容〉                              │
└───────────────┴─────────────────────────────────────────────┘
```

**權限控制**：
- `⚙ 設定`（改名、刪除）→ 只有 Owner 看得到
- `[👥 成員]` 分頁 → 所有人皆可見（檢視權）

---

### 3.3 `/projects/[projectId]/materials` — 資料管理

**責任**：上傳檔案、顯示清單、刪除檔案。

```
┌───────────────────────────────────────────────────────────┐
│  ┌─────────────────────────────────────────────────────┐  │
│  │  📤 拖拉檔案至此，或點擊選擇（PDF / DOCX / TXT / MD） │  │
│  │  最大 15 MB                                          │  │
│  └─────────────────────────────────────────────────────┘  │
│                                                           │
│  檔案清單（7 份）                                          │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ □  檔案名稱              大小    狀態       上傳者  操作│ │
│  │ ─────────────────────────────────────────────────── │ │
│  │ 📄 公司規則v2.pdf        1.2MB  ✅ 索引完成  Alice  🗑  │ │
│  │ 📄 技術規格書.pdf         800KB  ⏳ 索引中    Bob       │ │
│  │ 📄 Q3目標.docx           200KB  ❌ 索引失敗  Alice  🗑  │ │
│  └──────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────┘
```

**上傳暫存對話框**（使用者確認後才正式上傳）：
```
┌─ 確認上傳 ──────────────────────────────┐
│  以下檔案將上傳至「Q3 產品規劃」：        │
│                                        │
│  📄 新規則.pdf      1.2 MB    [✕ 移除] │
│  📄 附件.docx       300 KB   [✕ 移除]  │
│                                        │
│  上傳後將同步建立 AI 知識庫索引           │
│                                        │
│            [取消]  [確認上傳]            │
└────────────────────────────────────────┘
```

**元件樹**
```
MaterialsPage
├── UploadZone                ← react-dropzone
│   └── UploadStagingDialog   ← 暫存確認對話框
│       └── 上傳中 Progress   ← 三步驟進度（Storage→Dify→DB）
└── MaterialTable
    └── MaterialRow[]         ← GET /projects/:id/materials
        └── IndexingStatusBadge  ← 自動輪詢（5秒，直到非PROCESSING）
```

---

### 3.4 `/meetings` — 全局 Meetings 頁面

**責任**：跨專案整合顯示使用者所有相關會議（包含無關聯專案的獨立會議）。

```
┌───────────────────────────────────────────────────────────────┐
│  Meetings                              [+ 建立會議]            │
│  🔍 搜尋...  [全部▼（時間）]  [倒序▼]                         │
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │ 🟢 每週同步      進行中  Q3 產品規劃  2026-05-26 10:00  [>]│  │
│  │ ✅ 臨時會議      已結束  （無關聯專案）  2026-05-26 09:00 [>]│  │
│  │ ✅ 產品需求討論  已結束  行銷企劃      2026-05-25 14:00  [>]│  │
│  └─────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────┘
```

**元件樹**
```
AllMeetingsPage
├── MeetingFilterBar         ← search、since（時間篩選）、order
├── GlobalCreateMeetingDialog
└── MeetingList
    └── MeetingRow[]         ← GET /meetings（全局，含 projectName 欄位）
```

> **無關聯專案的會議**：從此頁進入詳情時，使用 `/meetings/:meetingId` 路由（非 `/projects/...`）。
> 詳情頁面功能與專案內的會議詳情相同，但頁面標頭不顯示專案名稱。

---

### 3.5 `/projects/[projectId]/meetings` — 專案會議清單

**責任**：顯示該專案的所有會議實例、建立新會議（自動關聯此專案）。

```
┌───────────────────────────────────────────────────────────┐
│  [+ 建立會議]                                              │
│  🔍 搜尋...  [全部▼（時間）]  [倒序▼]                     │
│                                                           │
│  ┌────────────────────────────────────────────────────┐  │
│  │ 🟢 每週同步      進行中   2026-05-26 10:00    [進入]│  │
│  │ ✅ 產品需求討論  已結束   2026-05-25 14:00~15:30 [>]│  │
│  │ ✅ Q2 回顧       已結束   2026-05-20 09:00~10:00 [>]│  │
│  └────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────┘
```

**建立會議對話框（專案內，無關聯專案選擇欄）**：
```
┌─ 建立新會議 ────────────────────────────────────────────┐
│                                                        │
│  會議名稱（選填）                                       │
│  ┌───────────────────────────────────────────────────┐│
│  │ 例：每週同步會議                                     ││
│  └───────────────────────────────────────────────────┘│
│                                                        │
│  Google Meet URL                                       │
│  ┌─────────────────────────────────┐ [建立新 Meet ↗]  │
│  │ 貼上 meet.google.com/... 連結...│                   │
│  └─────────────────────────────────┘                   │
│  ℹ️ 點擊「建立新 Meet」將在新分頁開啟 meet.new           │
│                                                        │
│                       [取消]  [建立並邀請蜜塔]           │
└────────────────────────────────────────────────────────┘
```

**全局建立會議對話框（Sidebar 入口，多出「關聯專案」欄）**：
```
┌─ 建立新會議 ────────────────────────────────────────────┐
│                                                        │
│  會議名稱（選填）                                       │
│  ┌───────────────────────────────────────────────────┐│
│  │ 例：每週同步會議                                     ││
│  └───────────────────────────────────────────────────┘│
│                                                        │
│  關聯專案（選填，留空建立獨立會議）                      │
│  ┌─────────────────────────────────────────────────┐  │
│  │ 搜尋或選擇專案...                         [▼]   │  │
│  └─────────────────────────────────────────────────┘  │
│                                                        │
│  Google Meet URL                                       │
│  ┌─────────────────────────────────┐ [建立新 Meet ↗]  │
│  │ 貼上 meet.google.com/... 連結...│                   │
│  └─────────────────────────────────┘                   │
│                                                        │
│                       [取消]  [建立並邀請蜜塔]           │
└────────────────────────────────────────────────────────┘
```

**元件樹**
```
MeetingsPage
├── MeetingFilterBar         ← search、since、order
├── CreateMeetingDialog      ← POST /projects/:id/meetings（projectId 固定）
└── MeetingList
    └── MeetingRow[]         ← GET /projects/:id/meetings
        └── BotStatusIndicator
```

---

### 3.6 `/projects/[projectId]/meetings/[meetingId]` — 會議詳情

此頁根據 `status` 有三種呈現模式：

**進行中（ACTIVE）：**
```
┌───────────────────────────────────────────────────────────┐
│ ← 返回   每週同步   🟢 進行中   10:05 開始       [結束會議]│
│ meet.google.com/abc-defg-hij                    [📋 複製] │
├───────────────────────────────────────────────────────────┤
│                                                           │
│  ── 即時逐字稿 ─────────────────────────── 自動更新中 🔄  │
│                                                           │
│  10:06  [Alice]   我們來討論這次的 Q3 目標                  │
│  10:08  [Bob]     蜜塔，請問去年 Q3 的目標是什麼？          │
│  10:08  [🤖 Bot]  根據資料，去年 Q3 目標為提升用戶留存率...  │
│  10:10  [Alice]   好的，那今年我們應該...                  │
│                                                ▼ 最新     │
└───────────────────────────────────────────────────────────┘
```

**已結束（ENDED）：**
```
┌───────────────────────────────────────────────────────────┐
│ ← 返回   產品需求討論   ✅ 已結束                           │
│ 2026-05-25  14:00 ~ 15:30  │  與會：Alice, Bob, 蜜塔（Meeta）│
├──────────────────┬────────────────────────────────────────┤
│  📋 摘要         │  🔖 交辦事項                            │
│                  │                                        │
│  本次會議討論了   │  ☐ Alice 本週五完成 API 文件            │
│  Q3 產品路線圖，  │  ☐ Bob 安排 wireframe review           │
│  確認了三個主要   │  ☐ 下次會議確認技術方案可行性           │
│  功能的優先順序…  │                                        │
├──────────────────┴────────────────────────────────────────┤
│  ── 完整逐字稿 ────────────────────────────────────────── │
│  14:00  [Alice]   大家好，今天主要討論...                   │
│  14:02  [Bob]     關於功能 A，我有個問題...                 │
└───────────────────────────────────────────────────────────┘
```

**摘要生成中（ENDED 但 summary 為 null）：**
```
│  📋 摘要                                                  │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  ⏳ 蜜塔正在生成會議摘要...（通常需要 10-30 秒）      │ │
│  └─────────────────────────────────────────────────────┘ │
```

**失敗（FAILED）：**
```
┌───────────────────────────────────────────────────────────┐
│ ← 返回   每週同步   ❌ 建立失敗                              │
├───────────────────────────────────────────────────────────┤
│                                                           │
│  ⚠️  Bot 建立失敗                                          │
│  蜜塔未能成功加入此會議（可能原因：服務重啟、網路問題）。       │
│  請返回專案頁面，重新邀請 Bot 加入新會議。                    │
│                                                           │
│  [← 返回專案]                                              │
└───────────────────────────────────────────────────────────┘
```
> FAILED 為終止態（無重試按鈕）：此 MeetingInstance 已無法恢復，
> 使用者需回到專案頁重新建立一個 MeetingInstance。

**元件樹**
```
MeetingDetailPage
├── MeetingHeader（名稱、狀態、時間、複製連結、結束按鈕）
│
├── [ACTIVE]  LiveTranscript       ← GET .../transcriptions?since_start_time=X（3秒輪詢）
│
├── [FAILED]  MeetingFailedBanner  ← 靜態錯誤提示，無輪詢
│
└── [ENDED]
    ├── MeetingSummary             ← GET .../meetings/:id（5秒輪詢直到 summary 填入）
    │   ├── SummaryPanel
    │   └── ActionItemsPanel
    └── TranscriptViewer           ← GET .../transcriptions（一次性載入）
```

---

### 3.7 `/projects/[projectId]/members` — 成員管理

```
┌───────────────────────────────────────────────────────────┐
│  成員（4 人）                        [+ 邀請成員]（Owner） │
│                                                           │
│  ┌────────────────────────────────────────────────────┐  │
│  │ 👑 Alice（你）  owner@example.com    所有者          │  │
│  │ ─────────────────────────────────────────────────  │  │
│  │ 👤 Bob          bob@example.com      ✅檢視  ✅編輯  ❌會議  🗑 │  │
│  │ 👤 Carol        carol@example.com    ✅檢視  ❌編輯  ✅會議  🗑 │  │
│  └────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────┘
```

**邀請成員對話框**：
```
┌─ 邀請成員 ─────────────────────────────┐
│                                        │
│  Email                                 │
│  ┌────────────────────────────────┐   │
│  │ member@example.com             │   │
│  └────────────────────────────────┘   │
│                                        │
│  初始權限                              │
│  ☑ 檢視權  ☐ 編輯權  ☐ 會議權          │
│                                        │
│            [取消]  [邀請]               │
└────────────────────────────────────────┘
```

---

### 3.8 `/projects/[projectId]/history` — 編輯歷史

```
┌───────────────────────────────────────────────────────────┐
│  編輯歷史                                                  │
│                                                           │
│  2026-05-26 14:30  Bob      🗑 刪除  公司規則v2.pdf        │
│  2026-05-26 10:00  Alice    📤 上傳  公司規則v2.pdf        │
│  2026-05-25 09:00  Alice    📤 上傳  技術規格書.pdf        │
└───────────────────────────────────────────────────────────┘
```

---

## 四、自訂 Hook 設計

```typescript
// ── 專案 ────────────────────────────────────────────

// 取得使用者的所有專案清單
function useProjects(): UseQueryResult<ProjectListResponse>

// 取得單一專案詳細資訊（含 permissions）
function useProject(projectId: string): UseQueryResult<ProjectDetail>

// 建立專案
function useCreateProject(): UseMutationResult

// 刪除專案
function useDeleteProject(): UseMutationResult

// ── 資料 ────────────────────────────────────────────

// 取得資料清單（第一次載入 + 手動重新整理）
function useMaterials(projectId: string): UseQueryResult<PaginatedMaterials>

// 上傳檔案（含 multipart/form-data）
function useUploadMaterial(projectId: string): UseMutationResult

// 刪除檔案
function useDeleteMaterial(projectId: string): UseMutationResult

// 單一材料的索引狀態輪詢（直到 COMPLETED/FAILED 才停止）
function useMaterialStatus(projectId: string, materialId: string) {
  return useQuery({
    queryKey: ['material', materialId],
    queryFn: () => api.getMaterial(projectId, materialId),
    refetchInterval: (query) => {
      const status = query.state.data?.indexingStatus
      return status === 'PROCESSING' || status === 'PENDING' ? 5000 : false
    }
  })
}

// ── 會議 ────────────────────────────────────────────

// 取得專案內會議清單
function useMeetings(projectId: string): UseQueryResult<PaginatedMeetings>

// 取得全局會議清單（/meetings 頁面，跨專案 + 無關聯專案）
function useAllMeetings(params?: {
  search?: string
  since?: 1 | 3 | 7
  order?: 'asc' | 'desc'
  status?: MeetingStatus
}): UseQueryResult<PaginatedMeetings>  // MeetingRow 含 projectId / projectName（可為 null）

// 取得單一會議（含摘要生成輪詢）
// projectId 為 null 時使用全局端點 GET /meetings/:id（無關聯專案的會議）
function useMeeting(projectId: string | null, meetingId: string) {
  return useQuery({
    queryKey: ['meeting', meetingId],
    queryFn: () => projectId
      ? api.getMeeting(projectId, meetingId)
      : apiClient.get<MeetingDetail>(`/meetings/${meetingId}`),
    refetchInterval: (query) => {
      const m = query.state.data
      // Bot 加入中：繼續輪詢
      if (m?.status === 'PENDING') return 3000
      // 會議進行中：繼續輪詢以偵測 ACTIVE → ENDED 狀態轉換
      // （Bot 自動離會後才知道 status 變 ENDED，父元件依此切換 isActive → false，停止逐字稿輪詢）
      if (m?.status === 'ACTIVE') return 5000
      // 摘要生成中：繼續輪詢直到 summary 不為 null
      // ⚠️ 條件必須用 === null，不可用 !m?.summary：
      //   後端在「無逐字稿」或「摘要生成失敗」時設 summary = ''（空字串作為 sentinel）。
      //   !'' 為 true，若用 !m?.summary 則空字串同樣觸發輪詢，輪詢永遠不停止。
      //   null  → 摘要尚未嘗試生成（繼續輪詢）
      //   ''    → 已嘗試但無內容（停止輪詢，UI 顯示「無摘要」）
      //   string → 有內容（停止輪詢）
      if (m?.status === 'ENDED' && (m?.summary === null || m?.summary === undefined)) return 5000
      // ENDED（summary 非 null，含空字串）或 FAILED：終止態，停止輪詢
      // FAILED 由 MeetingFailedBanner 處理，顯示靜態錯誤提示，無重試按鈕
      return false
    }
  })
}

// 進行中會議的即時逐字稿輪詢
// ⚠️ ACTIVE 會議的 API 使用 since_start_time（float）游標，非 since_id（integer）
// 04-API設計.md：GET .../transcriptions?since_start_time=X 回傳 start_time >= X 的 segments
// projectId 為 null 時使用全局端點 GET /meetings/:id/transcriptions（無關聯專案的會議）
//
// 去重說明：後端 filter 為 >=，cursor 邊界 segment 會被重複回傳；網路重試也可能帶重複。
// 使用 Map<segmentKey, segment> upsert 去重：
//   優先用 segmentId（Vexa segment_id，穩定唯一）；為 null 時退而用 `${startTime}-${text}`
// Map upsert 同時處理 Vexa mutable segment 更新（相同 ID 覆蓋舊值）。
function useLiveTranscriptions(
  projectId: string | null,
  meetingId: string,
  isActive: boolean   // 只有 ACTIVE 才輪詢
) {
  const [sinceStartTime, setSinceStartTime] = useState<number | null>(null)
  const [segmentMap, setSegmentMap] = useState<Map<string, TranscriptSegment>>(new Map())

  useQuery({
    queryKey: ['transcriptions-live', meetingId],
    queryFn: async () => {
      const basePath = projectId
        ? `/projects/${projectId}/meetings/${meetingId}/transcriptions`
        : `/meetings/${meetingId}/transcriptions`
      const url = sinceStartTime !== null
        ? `${basePath}?since_start_time=${sinceStartTime}`
        : basePath
      const res = await apiClient.get<TranscriptResponse>(url)
      if (res.items.length > 0) {
        setSegmentMap(prev => {
          const next = new Map(prev)
          for (const seg of res.items) {
            const key = seg.segmentId ?? `${seg.startTime}-${seg.text}`
            next.set(key, seg)
          }
          return next
        })
        setSinceStartTime(res.items.at(-1)!.startTime)  // float（Vexa 的 start_time）
      }
      return res
    },
    enabled: isActive,
    refetchInterval: 3000,
  })

  // 依 startTime 排序後輸出，保持逐字稿顯示順序一致
  return useMemo(
    () => [...segmentMap.values()].sort((a, b) => a.startTime - b.startTime),
    [segmentMap],
  )
}

// ── 權限 ────────────────────────────────────────────

// 從 project 資料推導當前使用者的操作權限
function usePermissions(projectId: string): UserPermissions {
  const { data: project } = useProject(projectId)
  return project?.permissions ?? {
    canView: false, canEdit: false,
    canDelete: false, canManage: false, canMeeting: false
  }
}
```

---

## 五、API Client 實作

```typescript
// lib/api-client.ts
import { getSession } from 'next-auth/react'

const BASE_URL = process.env.NEXT_PUBLIC_API_URL ?? 'http://localhost:4000'

async function getAuthHeader(): Promise<HeadersInit> {
  const session = await getSession()
  // session.vexaToken 由 NextAuth session callback 注入（Vexa 原本即有）
  const token = (session as any)?.vexaToken
  if (!token) throw new Error('Not authenticated')
  return { Authorization: `Bearer ${token}` }
}

export const apiClient = {
  async get<T>(path: string): Promise<T> {
    const headers = await getAuthHeader()
    const res = await fetch(`${BASE_URL}${path}`, { headers })
    if (!res.ok) throw await res.json()
    return res.json()
  },

  async post<T>(path: string, body?: unknown): Promise<T> {
    const headers = await getAuthHeader()
    const res = await fetch(`${BASE_URL}${path}`, {
      method: 'POST',
      headers: { ...headers, 'Content-Type': 'application/json' },
      body: body ? JSON.stringify(body) : undefined,
    })
    if (!res.ok) throw await res.json()
    return res.json()
  },

  async postForm<T>(path: string, formData: FormData): Promise<T> {
    const headers = await getAuthHeader()  // 注意：multipart 不設 Content-Type，讓瀏覽器自動設
    const res = await fetch(`${BASE_URL}${path}`, {
      method: 'POST',
      headers,  // 不包含 Content-Type
      body: formData,
    })
    if (!res.ok) throw await res.json()
    return res.json()
  },

  async patch<T>(path: string, body: unknown): Promise<T> {
    const headers = await getAuthHeader()
    const res = await fetch(`${BASE_URL}${path}`, {
      method: 'PATCH',
      headers: { ...headers, 'Content-Type': 'application/json' },
      body: JSON.stringify(body),
    })
    if (!res.ok) throw await res.json()
    return res.json()
  },

  async delete(path: string): Promise<void> {
    const headers = await getAuthHeader()
    const res = await fetch(`${BASE_URL}${path}`, { method: 'DELETE', headers })
    if (!res.ok) throw await res.json()
  },
}
```

---

## 六、App Shell 與認證守衛

```typescript
// app/(app)/layout.tsx
import { getServerSession } from 'next-auth'
import { redirect } from 'next/navigation'
import { authOptions } from '@/lib/auth'
import { Sidebar } from '@/components/layout/sidebar'

export default async function AppLayout({ children }: { children: React.ReactNode }) {
  const session = await getServerSession(authOptions)

  // 未登入 → 重導向到登入頁
  if (!session) redirect('/login')

  return (
    <div className="flex h-screen">
      <Sidebar />
      <main className="flex-1 overflow-auto">
        {children}
      </main>
    </div>
  )
}
```

---

## 七、PermissionGuard 元件

用於條件式渲染需要特定權限才能看到的 UI：

```typescript
// components/permission-guard.tsx
'use client'
import { usePermissions } from '@/hooks/use-permissions'
import type { UserPermissions } from '@/types/api'

interface Props {
  projectId: string
  require: keyof UserPermissions
  children: React.ReactNode
  fallback?: React.ReactNode
}

export function PermissionGuard({ projectId, require, children, fallback = null }: Props) {
  const permissions = usePermissions(projectId)
  if (!permissions[require]) return <>{fallback}</>
  return <>{children}</>
}
```

**使用範例：**
```tsx
// 只有有刪除權的使用者才看得到「刪除專案」按鈕
<PermissionGuard projectId={projectId} require="canDelete">
  <Button variant="destructive" onClick={handleDelete}>刪除專案</Button>
</PermissionGuard>

// 只有有管理權的使用者才看得到「邀請成員」按鈕
<PermissionGuard projectId={projectId} require="canManage">
  <InviteMemberDialog />
</PermissionGuard>
```

---

## 八、IndexingStatus Badge 元件

材料列表中每筆資料的狀態 badge，對 `PENDING/PROCESSING` 狀態自動輪詢：

```typescript
// components/materials/indexing-status-badge.tsx
'use client'
import { useMaterialStatus } from '@/hooks/use-materials'
import { Badge } from '@/components/ui/badge'

const STATUS_CONFIG = {
  PENDING:    { label: '等待中',  variant: 'secondary', icon: '⏳' },
  PROCESSING: { label: '索引中',  variant: 'outline',   icon: '🔄' },
  COMPLETED:  { label: '索引完成', variant: 'success',   icon: '✅' },
  FAILED:     { label: '索引失敗', variant: 'destructive', icon: '❌' },
} as const

export function IndexingStatusBadge({ projectId, materialId, initialStatus }) {
  const { data } = useMaterialStatus(projectId, materialId)
  const status = data?.indexingStatus ?? initialStatus

  const config = STATUS_CONFIG[status]
  return (
    <Badge variant={config.variant}>
      {config.icon} {config.label}
    </Badge>
  )
}
```

---

## 九、環境變數（前端）

```bash
# .env.local

# 後端 API URL
NEXT_PUBLIC_API_URL="http://localhost:4000"

# NextAuth（與 Vexa 共用）
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="..."

# Vexa API（NextAuth signIn 時使用）
VEXA_API_URL="http://localhost:8056"
VEXA_ADMIN_API_URL="http://localhost:8057"
VEXA_ADMIN_API_KEY="..."

# Google OAuth（啟用 Google 登入）
GOOGLE_CLIENT_ID="..."
GOOGLE_CLIENT_SECRET="..."
```

---

## 十、輪詢策略總覽

| 場景 | Hook | 頻率 | 停止條件 |
|------|------|------|---------|
| 檔案索引狀態 | `useMaterialStatus` | 5 秒 | status ∉ {PENDING, PROCESSING} |
| Bot 加入中 | `useMeeting` | 3 秒 | status ≠ PENDING |
| 會議進行中（偵測結束） | `useMeeting` | 5 秒 | status ≠ ACTIVE |
| 即時逐字稿 | `useLiveTranscriptions` | 3 秒 | 父元件傳入 `isActive=false`（由 `useMeeting` 回傳 ENDED 觸發）；Map 去重，cursor 用 >= 過濾 |
| 摘要生成中 | `useMeeting` | 5 秒 | summary 不為 null（含空字串 ''；後端在無逐字稿或失敗時設 ''） |

---

*文件結尾*
