# 開發環境設定

|項目|內容|
|----|-----|
|文件版本|v1.0|
|撰寫日期|2026-05-30|
|執行時機|**P1 實作開始前，人工完成一次**|

> 此文件為人工操作清單，非 agent 讀取文件。完成所有步驟後才能開始 Phase 1。

---

## 一、本機環境需求

| 工具 | 版本需求 | 用途 |
|------|---------|------|
| Node.js | 20+ | 後端執行環境 |
| Docker Desktop | 最新穩定版 | 運行 vexa-lite |
| Git | 任意 | 版本控制 |

---

## 二、外部服務申請

完成前先取得以下帳號與 API Key：

| 服務 | 用途 | 取得位置 |
|------|------|---------|
| Supabase | PostgreSQL + Storage | supabase.com → 建立新 Project |
| Dify | RAG Q&A Chatflow + 摘要 Workflow | dify.ai（雲端）或自架 |
| Anthropic | Claude API（獨立會議 Q&A fallback） | console.anthropic.com |

---

## 三、步驟

### 3.1 啟動 vexa-lite

執行 `docs/09-實作計畫/00-vexa-lite-local.md` 中的指令（含憑證，已加入 `.gitignore`）：

1. 執行 **§1 docker run 指令**，等待容器啟動（約 1–2 分鐘）
2. 執行 **§2 init-db**，初始化 Supabase 的 `public` schema
3. 執行 **§3 確認啟動**，確認容器狀態為 `Up`

> vexa-lite 啟動並執行 init-db 後，Supabase 的 `public` schema 會建立
> `users`、`meetings`、`transcriptions` 等表。

### 3.2 Supabase 初始設定

**建立 `app` schema**（在 Supabase SQL Editor 執行）：

```sql
CREATE SCHEMA IF NOT EXISTS app;
GRANT ALL ON SCHEMA app TO postgres;
GRANT USAGE ON SCHEMA app TO authenticated;
```

**建立 Storage Bucket**（在 Supabase Dashboard → Storage → New Bucket）：

| 設定項 | 值 |
|-------|-----|
| Bucket name | `meeting-materials` |
| Public | 否（Private） |
| File size limit | `15` MB |
| Allowed MIME types | `application/pdf, application/vnd.openxmlformats-officedocument.wordprocessingml.document, text/plain, text/markdown` |

### 3.3 Dify 設定

1. **匯入 Q&A Chatflow**：Dify 平台 → Studio → Import DSL → 選擇 `docs/01-edu2.yml`
2. **設定 Chatflow 的 API Key**（擇一）：
   - 在 Dify 平台 Settings → Environment Variables 新增 `DIFY_DATASET_API_KEY = dataset-...`
   - 或直接在 HTTP Request node 的 headers 欄位填入實際 Key
3. **建立會議摘要 Workflow**：依 `docs/01-RAG_API_串接文件_v1.1.md` 說明建立
4. **取得四把 API Key**：

| 變數名 | 格式前綴 | 對應用途 |
|--------|---------|---------|
| `DIFY_DATASET_API_KEY` | `dataset-` | Knowledge Base 操作 |
| `DIFY_WORKFLOW_API_KEY` | `app-` | RAG Q&A Chatflow |
| `DIFY_SUMMARY_WORKFLOW_API_KEY` | `app-` | 檔案摘要 Workflow |
| `DIFY_MEETING_SUMMARY_WORKFLOW_API_KEY` | `app-` | 會議摘要 Workflow |

### 3.4 後端 .env 設定

```bash
cd backend
cp .env.example .env
```

填入 `.env`（所有欄位均必填，見 `docs/03-資料庫Schema設計.md §六` 與 `docs/04-API設計.md §十三`）：

```bash
# Supabase
DATABASE_URL="postgresql://postgres:[PWD]@db.[PROJ].supabase.co:5432/postgres?schema=app"
SUPABASE_URL="https://[PROJ].supabase.co"
SUPABASE_SERVICE_ROLE_KEY="..."
SUPABASE_STORAGE_BUCKET="meeting-materials"

# Dify
DIFY_API_BASE="https://api.dify.ai/v1"
DIFY_DATASET_API_KEY="dataset-..."
DIFY_WORKFLOW_API_KEY="app-..."
DIFY_SUMMARY_WORKFLOW_API_KEY="app-..."
DIFY_MEETING_SUMMARY_WORKFLOW_API_KEY="app-..."
DIFY_CHATFLOW_TIMEOUT_MS=45000

# Anthropic
ANTHROPIC_API_KEY="sk-ant-..."

# Vexa WebSocket
VEXA_WS_URL="ws://localhost:8056"   # vexa-lite 的 WebSocket 端點

# Server
APP_PORT=4000
APP_CORS_ORIGINS="http://localhost:3000"
```

### 3.5 Prisma 初始化（需 vexa-lite 運行中）

```bash
cd backend

# 1. 同步 Vexa 的 public schema 定義
npx prisma db pull
# ⚠️ 執行後務必 diff，只保留 User / Meeting / Transcription 三個 model
# 其他 Vexa model 一律刪除，確認 app schema models 未被覆蓋

# 2. 建立 app schema 的初始 migration
npx prisma migrate dev --name init_app_schema
```

---

## 四、驗收清單

完成所有步驟後，確認以下項目後才開始 Phase 1：

- [ ] `docker compose ps` 所有 vexa-lite 服務顯示 `Up`
- [ ] Supabase Dashboard 可見 `app` schema 與 `public` schema
- [ ] Supabase Storage 存在 `meeting-materials` bucket
- [ ] `npx prisma migrate status` 顯示 migration 已套用
- [ ] Dify Q&A Chatflow 已匯入，且 `DIFY_DATASET_API_KEY` 已設定
- [ ] `.env` 所有欄位均已填入實際值（無空欄位）

---

## 五、各 Phase 的 Docker 需求

| Phase | 需要 vexa-lite 運行？ | 說明 |
|-------|---------------------|------|
| P1 | **一次性**（init 期間） | prisma db pull + migrate 需要，完成後可關閉 |
| P2–P3 | 否 | 單元測試全 mock；Smoke test 只需 Supabase + Dify（雲端） |
| P4–P7 | **是** | Bot API、WebSocket、逐字稿都依賴 vexa-lite |

> **P4 開始前**：確認 vexa-lite 重新啟動並正常運行，後續保持開著直到 P7 完成。

---

*文件結尾*

---

## Google OAuth 設定（登入＋一鍵開會）

> 2026-07 起，「一鍵開會並邀請蜜塔」需要 Google Calendar API（敏感權限 `calendar.events`）。
> 未經驗證的 OAuth app 在「正式版」狀態下會被 Google 直接擋（403 access_denied），
> **同意畫面務必維持「測試中」並把全組 email 加為測試使用者**。

### 建立 OAuth Client（任一組員的 GCP 專案皆可，一次性）

1. [console.cloud.google.com](https://console.cloud.google.com) → 選定/新建專案
2. 「API 和服務 → 程式庫」→ 搜尋 **Google Calendar API** → 啟用
3. 「API 和服務 → OAuth 同意畫面」：
   - User Type：**外部** → App 名稱（例：MeetBot）、支援 email 填自己 → 儲存
   - 發布狀態：**測試中**（不要發布正式版）
   - 測試使用者：**加入全組每個人的 Gmail**（沒加的人無法登入）
4. 「API 和服務 → 憑證 → 建立憑證 → OAuth 用戶端 ID」：
   - 類型：**網頁應用程式**
   - 已授權的 JavaScript 來源：`http://localhost:3000`
   - 已授權的重新導向 URI：`http://localhost:3000/api/auth/callback/google`
5. 取得 Client ID / Secret → 填入每人自己的 `frontend/.env.local`：
   `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET`

### 注意事項

- **登入鑄 token 已與 Vexa 容器脫鉤（2026-07-08）**：在 `backend/.env` 與
  `frontend/.env.local` 設定**同值的** `INTERNAL_AUTH_SECRET`（隨機字串 ≥16 字），
  登入即改走後端 `/internal/token` 直接對 DB get-or-create，**不需要 Docker**。
  未設定此變數則退回舊路（docker exec 進 vexa-lite 容器），屆時容器必須在跑，
  否則登入後全站 401（症狀：「專案清單載入失敗」）。
- 換過 client 或新增 scope 後，**每個人要登出重登一次**；同意畫面出現
  「未經 Google 驗證」警告是測試模式的正常現象（進階 → 繼續）。
- 使用者在同意畫面拒絕日曆權限：登入仍成功，僅一鍵開會退化為手動貼連結。
- Google access token 效期約 1 小時，過期時一鍵開會會提示重新登入（未做 refresh rotation）。
