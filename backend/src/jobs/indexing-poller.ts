import { logger } from '../middleware/logger.js'

// TODO: P3 — poll Dify every 30 seconds to update indexing_status for PROCESSING materials
export function startIndexingPoller(): void {
  logger.info('Indexing poller stub — will be implemented in P3')
}
